# UnitConverter_29 — Activity 2·3 TDD GREEN 전체

| 항목 | 내용 |
|------|------|
| 프로젝트 | Unit Converter — 길이 단위 변환 (Python) |
| 워크북 ID | UnitConverter_29 |
| 단계 | Activity 2·3 — 품질 요구사항 구현 + TC 구현 (TDD GREEN) |
| 작성일 | 2026-06-11 |
| 출처 | [`Report/02...`](02.UnitConverter_29-Activity3-입력검증-RED-스켈레톤-보고서.md) · Cursor 채팅 세션 |

---

## 1. 세션 요약

입력 검증 RED 스켈레톤 4건을 **1 RED 묶음당 1 commit**으로 GREEN한 뒤, README Dual-Track 표를 보강하고 추가 테스트(엣지·happy path·변환·포맷) 17건을 확장했다. 남은 RED 7건(`T-LOGIC-001~002, 005, 012`, `T-UI-003~005`)도 동일 원칙으로 최소 구현하여 **Dual-Track 21건 전부 GREEN** (`21 passed`)을 달성했다.

---

## 2. 핵심 산출물

| 산출물 | 경로·상태 |
|--------|-----------|
| Domain — 검증 | `src/domain/input_validator.py`, `conversion_registry.py`, `errors.py` |
| Domain — 변환 | `src/domain/length_converter.py`, `constants.py` |
| Application — DTO | `src/application/convert_length_use_case.py` |
| Boundary — 파싱·출력·CLI | `src/boundary/input_parser.py`, `text_formatter.py`, `json_formatter.py`, `cli_controller.py` |
| 테스트 (21건) | `tests/entity/` (13), `tests/boundary/` (10) |
| Dual-Track SSOT | `README.md` — 21 Test ID ✅ 반영 |
| GREEN 커밋 | 11건 (입력 검증 4 + 확장 7) |

---

## 3. GREEN 커밋 이력 (1 RED 묶음 = 1 commit)

| # | Commit | Test ID | 구현 |
|---|--------|---------|------|
| 1 | `cb51ce2` | T-LOGIC-003 | 음수 검증 |
| 2 | `2e951ac` | T-LOGIC-004 | `ConversionRegistry` + 미등록 단위 |
| 3 | `1bce246` | T-LOGIC-006 | 숫자 변환 실패 |
| 4 | `a20fb39` | T-UI-002 | `InputParser` 형식 오류 |
| 5 | `a7e031c` | T-LOGIC-001 | `LengthConverter` + `constants.py` |
| 6 | `867e858` | T-LOGIC-002 | feet → meter |
| 7 | `44c2477` | T-LOGIC-012 | `register()` |
| 8 | `69816cb` | T-LOGIC-005 | cubit 동적 등록·변환 |
| 9 | `f7b8587` | T-UI-003 | `TextFormatter` |
| 10 | `0f4e7cd` | T-UI-004 | `JsonFormatter` |
| 11 | `874b8c9` | T-UI-005 | `CliController` + DTO |

---

## 4. Dual-Track · PRD 매핑

### Track B — Logic (13건 ✅)

| Test ID | PRD | 클래스 | Given → Then |
|---------|-----|--------|--------------|
| T-LOGIC-001~002 | 기본 4번, 비즈니스 로직 | `LengthConverter` | 변환 정확도 |
| T-LOGIC-003~004, 006~010 | 품질 — 입력 검증 | `InputValidator` | 음수·단위·숫자·경계·happy |
| T-LOGIC-005, 011~012 | 추가 — 동적 등록 | `ConversionRegistry` | register·is_registered |

### Track A — UI (8건 ✅)

| Test ID | PRD | 클래스 | Given → Then |
|---------|-----|--------|--------------|
| T-UI-001~002, 006~009 | 기본 1번, 품질 — 형식 | `InputParser` | 파싱·엣지 |
| T-UI-003~004 | 추가 — 출력 포맷 | `TextFormatter`, `JsonFormatter` | 텍스트·JSON |
| T-UI-005 | CLI 흐름 | `CliController` | Mock Use Case 연동 |

### PRD 커버리지 (현재)

| PRD 영역 | 상태 |
|----------|------|
| 품질 — 입력 검증 (음수·형식·없는 단위·숫자) | ✅ 100% |
| 기본 — 변환 정확도 | ✅ T-LOGIC-001~002 |
| 기본 — `meter:2.5` 파싱 | ✅ T-UI-001 |
| 추가 — 동적 등록 (cubit) | ✅ T-LOGIC-005 |
| 추가 — JSON/표 출력 | ✅ T-UI-003~004 |
| 추가 — 설정 외부화 | ⏳ 미구현 |
| `UnitConverter.py` CLI 연동 | ⏳ 레거시 `main()` 유지 |

---

## 5. pytest 결과

```bash
pytest -q
# 21 passed in 0.05s
```

| Track | 테스트 수 | 결과 |
|-------|-----------|------|
| B — Logic (`tests/entity/`) | 13 | 13 passed |
| A — UI (`tests/boundary/`) | 8 | 8 passed |
| **합계** | **21** | **21 passed** |

---

## 6. 성공 기준 / 검증

| # | 기준 (Activity 2·3) | 결과 |
|---|---------------------|------|
| SC-1 | OCP/SRP 클래스 구현 (Domain·Boundary·Application) | ✅ |
| SC-2 | 입력값 검증 구현 + TC | ✅ |
| SC-3 | 변환 정확도 TC | ✅ |
| SC-4 | Dual-Track 21건 GREEN | ✅ |
| SC-5 | 1 RED 묶음 = 1 commit | ✅ (11 commits) |
| SC-6 | `ConvertLengthUseCase.execute()` 실구현 | ⏳ DTO만 |
| SC-7 | `UnitConverter.py` 리팩토링 연동 | ⏳ |

---

## 7. 다음 단계

- `ConvertLengthUseCase.execute()` — Validator + Converter 조합
- `UnitConverter.py` → `CliController` 위임
- `ConfigLoader` + 설정 외부화 (Activity 4)
- `CsvFormatter` 추가 (Activity 4)

---

*Transcript: [`Prompting/03.UnitConverter_29-Activity2-3-TDD-GREEN-Export-Transcript.md`](../Prompting/03.UnitConverter_29-Activity2-3-TDD-GREEN-Export-Transcript.md)*
